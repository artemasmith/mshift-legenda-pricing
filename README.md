# mshift-legenda-pricing

Synthetic twin repository used to verify the MShift Acceptance Engine on a staging
stand. It contains no customer code: a single pricing module and two conventional
tests, mirroring the built-in benchmark case `BM-PY-0001` (discount double-apply).

Run: `python -m pytest -q`
